# Dubash-Exim

Dubash Exim Academy website built with React, Vite, Express, and TypeScript.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in `.env.local` if required.
3. Run the app:
   `npm run dev`
